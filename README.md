# Event Loop Trainer ⟳

Интерактивный тренажёр по JavaScript Event Loop. Соберите порядок, в котором появятся вызовы `console.log`, и разберите, как синхронный код, микрозадачи и макрозадачи попадают в очереди.

## Возможности

- Случайно генерирует уникальные задачи из готовых блоков кода.
- Использует синхронный код, `setTimeout`, `Promise.then`, `Promise.finally`, `async/await` и `queueMicrotask`.
- Позволяет собрать прогноз из перемешанных значений вывода, исправить его до проверки и сразу увидеть ошибки.
- Сохраняет статистику текущей серии, рекорда, решённых задач и последние ошибки в браузере.
- После проверки показывает текстовый разбор порядка выполнения.
- Визуализирует Event Loop: Call Stack, очередь микрозадач, очередь `setTimeout` и консоль. На каждом шаге подсвечивается соответствующая строка кода и объясняется причина перехода.
- Позволяет выбрать тематическую тренировку: микрозадачи, таймеры или `async/await`.

Эталонный порядок определяется фактическим выполнением сгенерированного сниппета с перехваченным `console.log`, а не заранее заданным ответом.

## Уровни сложности

| Уровень | Содержимое |
| --- | --- |
| **Easy** | Синхронный код, таймеры и простые микрозадачи. В задаче гарантированно есть и микро-, и макрозадача. |
| **Medium** | `async/await`, вложенные колбэки, цепочки промисов и более сложные сочетания очередей. |
| **Hard** | Объёмные комбинации вложенных таймеров, микрозадач и `async/await`; до 16 строк вывода. |

## Как пользоваться

1. Выберите уровень, тему или создайте новую задачу.
2. Прочитайте код и нажимайте на значения в порядке ожидаемого вывода.
3. Нажмите «Проверить».
4. Откройте «разбор» или «анимация event loop», чтобы понять правильный порядок; при ошибке задачу можно повторить кнопкой «ошибки».

## Локальный запуск

Требуется Node.js 22 или новее.

```bash
npm install
npm run dev
```

После запуска Vite выведет адрес локального сервера (по умолчанию — `http://localhost:5173`).

### Другие команды

```bash
npm run check-types  # проверка TypeScript без генерации файлов
npm run lint         # ESLint
npm run build        # production-сборка в dist/
npm run preview      # просмотр production-сборки
```

Перед коммитом Husky запускает `check-types` и `lint`.

## Технологии

- React 18
- TypeScript для компонентов и типов, JavaScript для генератора и данных
- Vite 5
- CSS Modules и CSS-переменные
- ESLint 9 и Husky

## Структура проекта

```text
├── .github/workflows/deploy.yml  # GitHub Pages: сборка и деплой из main
├── .husky/pre-commit             # проверка типов и линт перед коммитом
├── src/
│   ├── main.tsx                  # точка входа React
│   ├── App.tsx                   # состояние задачи, ответа и статистики
│   ├── components/               # UI тренажёра и CSS-модули
│   │   ├── EventLoopViz/         # пошаговая визуализация очередей
│   │   ├── TaskCodePanel/        # отображение и подсветка кода
│   │   ├── TokenPicker/          # выбор порядка вывода
│   │   ├── AnswerConsole/        # прогноз и результаты проверки
│   │   └── TaskExplanation/      # текстовый разбор
│   ├── data/
│   │   ├── blocks/               # строительные блоки сниппетов
│   │   ├── levels/               # настройки сложности
│   │   └── phases/               # названия фаз и подсказки
│   ├── lib/
│   │   ├── generator/            # генерация и фактическое выполнение задач
│   │   ├── sim/                  # шаги для анимации Event Loop
│   │   └── random/               # случайный выбор и перемешивание
│   ├── styles/                   # глобальные стили и общие элементы управления
│   └── types/                    # общие TypeScript-типы
├── eslint.config.js
├── tsconfig.json
└── vite.config.js
```

## Деплой

Workflow GitHub Actions собирает проект при каждом пуше в `main` и публикует папку `dist` в GitHub Pages. Базовый путь сборки задан как `/EventLoopTrainer/`; если имя репозитория изменится, обновите `base` в `vite.config.js`.
